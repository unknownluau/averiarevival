import { useEffect, useState } from "react";
import { createUseStyles } from "react-jss"

const useStyles = createUseStyles({

})

export default props => {
    const s = useStyles();
    const [gameSort, setGameSort] = useState(null);

    useEffect(() => {
        
    }, [])
}